apiVersion: apps/v1
kind: Deployment
metadata:
  name: priorix-core
  namespace: priorix
spec:
  replicas: 2
  selector:
    matchLabels:
      app: priorix-core
  template:
    metadata:
      labels:
        app: priorix-core
    spec:
      containers:
        - name: priorix-core
          image: ghcr.io/<your-github-owner>/priorix-core:latest
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: priorix-core-config
            - secretRef:
                name: priorix-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: priorix-core
  namespace: priorix
spec:
  selector:
    app: priorix-core
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
